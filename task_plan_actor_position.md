# mobile_app 演员管理列表位置修复

## 目标

修复演员管理中选择演员执行“同步演员关联”后，返回列表时滚动位置回到顶部的问题；行为与系列管理、分类管理一致，完成编辑/刷新后保持原始列表位置。

## 假设与边界

- 用户所说的 `mobile_app` 是仓库内 Flutter 客户端，不修改 `frontend_new` Web 端。
- “返回演员管理页面”实际是从同步编辑弹层关闭后回到仍在内存中的列表页；需要保留页面滚动控制器位置，同时在刷新数据后继续使用该位置。
- 只修改演员管理及其同步回调/公共列表位置逻辑，避免扩大范围。

## 阶段

- [completed] 定位演员、系列、分类管理的滚动状态和同步完成回调（已确认演员页缺少资源页已有的滚动恢复逻辑）
- [completed] 实施最小修复并补充回归测试
- [in_progress] 运行 Dart 格式化、分析和相关测试

## 成功标准

- 演员同步成功后刷新演员列表，滚动位置保持在同步前的位置。
- 系列、分类等现有列表行为不改变。
- 新增或调整的回归测试覆盖刷新后位置保持。

## 错误记录

| 错误 | 尝试 | 处理 |
|---|---|---|
| 在仓库根目录读取 Flutter 相对路径失败 | 1 | 已确认后续命令统一使用 `mobile_app` 作为工作目录 |
| 读取同步弹层时工作目录参数未生效，重复命中仓库根目录 | 2 | 后续改用绝对路径读取，避免再次重复该方法 |
| 回归测试固定点击 `演员 10`，该行在测试滚动位置不在当前构建树中 | 1 | 改为点击滚动后当前可见的演员名称文本，避免依赖固定行高 |
| 回归测试未关闭隐私遮罩，演员名称被替换为占位符 | 1 | 测试注入关闭隐私的 Notifier |
| `privacyShieldProvider.overrideWith` 误传 bool，类型要求 Notifier | 1 | 增加测试用 `PrivacyShieldNotifier` 子类并返回 false |
| `WidgetTester.pageBack` 依赖 Cupertino 返回按钮，详情页使用自定义返回图标 | 1 | 测试改为点击详情页的 `Icons.arrow_back` |
