# dbo 播放选源交互改造

## 目标

点击 dbo 模块的“在线播放”后，在当前页面底部弹出播放源选择栏；移除独立选源页面及其导航入口，保留现有播放源选择与播放能力。

## 阶段

- [completed] 梳理 dbo 播放入口、选源页面、路由和测试覆盖
- [completed] 实现底部选源栏并移除独立页面导航
- [completed] 更新/补充测试并完成静态检查与构建验证

## 成功标准

1. dbo 播放入口不再导航到独立选源页面，而是在当前页面底部展示选源 UI。
2. 选源栏支持现有源选择、关闭/返回和播放行为，不破坏已选源状态。
3. 独立选源页面及无用路由/导入被移除，项目分析与相关测试通过。

## 错误记录

| 错误 | 尝试 | 处理 |
|---|---:|---|
| `db_online_search_page.dart` 缺少 `AppL10n` getter 和搜索结果组件 | 1 | 属于工作区既有 DBO 搜索改动，不在本次播放交互范围；记录并在验证时排除该搜索页测试 |

---

# 当前任务：修复 DBO 隐私模式

## 阶段

- [completed] 让共享隐私揭示能力支持 DBO 的字符串影片标识
- [completed] 让 DBO 影片卡片在首页、最新、搜索及关联推荐中使用共享隐私模式
- [completed] 补测试并完成静态检查

## 成功标准

1. 隐私模式开启时，所有 `DbOnlineMovieCard` 的海报、番号、标题和元数据均被遮罩。
2. 未揭示卡片首次点击只揭示当前 DBO 影片，第二次点击才执行原详情回调。
3. OMM 现有整数影片/演员隐私行为不回归，相关测试通过。

## 错误记录

| 错误 | 尝试 | 处理 |
|---|---:|---|
| DBO 卡片改为读取 Riverpod 后，旧 widget 测试报 `Bad state: No ProviderScope found` | 1 | 在 DBO 卡片测试中注入 `ProviderScope` 和隐私 Provider 覆盖 |
| 隐私 widget 测试直接断言标题不存在，但 `PrivacyMask` 会保留被模糊的 Poster 子树 | 1 | 改为断言可见占位文本和锁图标，避免把渲染树中的被遮罩内容误判为泄露 |

## 当前任务：底部毛玻璃面板控件一致性源码审计

- [completed] 限定检查范围为带统一拖拽把手的 `showGlassSheet` / `showAppActionSheet` 面板
- [completed] 按选择/排序、表单、批量、进度/同步、播放源/资源浏览分组检查控件样式与操作逻辑
- [completed] 记录源码行号、差异、优先级，并区分语义色与真正的设计漂移
- [pending] 如用户确认实施，再抽取公共控件 token/组件并逐组改造

## 审计限制

- 未使用浏览器、运行态预览或截图，因此不能对最终渲染像素、平台字体替换和系统 Material 默认值做视觉定论。
