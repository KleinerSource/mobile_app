# 播放器能力统一改造计划

## 目标

在 `dev` 分支中深度梳理并优化 `ksplayer` 的执行逻辑，对标 `media_kit` 的播放器交互与能力边界，统一两种后端的控制体验：左侧滑动调节亮度、右侧滑动调节音量、长按快进、横向滑动调节进度，并确保状态、生命周期和控制层行为一致。

## 成功标准

- `ksplayer` 和 `media_kit` 对外暴露一致的核心播放能力与交互语义。
- 手势不互相误触：左右区域与横向进度/长按行为有明确仲裁，拖拽过程实时反馈，抬手后状态稳定。
- 播放、暂停、进度、音量、亮度、倍速和生命周期切换不会产生重复监听、状态回写冲突或资源泄漏。
- 为新增或修正的逻辑补充可执行的测试/静态验证，并完成项目现有构建或测试命令。

## 阶段

- [completed] 1. 盘点项目结构、播放器抽象、依赖和当前工作区状态
- [completed] 2. 对比 `ksplayer` 与 `media_kit` 的执行逻辑和交互实现，记录差异
- [completed] 3. 设计并实现统一能力层与手势仲裁/状态同步
- [completed] 4. 补充测试并运行格式化、静态检查、构建或测试
- [completed] 5. 复核差异、更新文档记录并交付

## 约束与决策

- 只修改直接服务于本次播放器统一目标的代码，不顺手重构无关模块。
- 优先复用后端原生能力；只有在后端缺少等价能力时才增加适配层。
- 手势交互遵循直接操控、连续反馈、可中断和无障碍降级原则。

## 实现决策

- `KSPlayer 2.3.4` 的左亮度、右音量、横向 seek、长按 2x 全部保留原生实现；Flutter 不为 KSPlayer 复制 `PlayerGestureLayer`。
- 统一层先落在两种后端共同的页面生命周期语义：系统音量提示、应用亮度恢复、后台暂停/恢复、seek 播放态保持、事件在离场后失效。
- 不在本次范围内重做 KSPlayer 原生控制条的视觉配置；其原生控制条已经覆盖播放、进度、倍速、清晰度、音轨和字幕入口。

## 错误记录

| 错误 | 尝试 | 解决 |
| --- | --- | --- |
| 精确拉取 `KSPlayer` revision 的命令在首次 clone 后超时，临时仓库只有未检出的对象 | 1 | 保留临时 clone，拆分为单独 fetch/checkout |
| 初始 clone 的后台 git 进程仍持有临时仓库锁，后续 checkout 报 `index.lock` | 1 | 不删除锁或终止进程，先等待后台 clone 完成后再检查状态 |
| 追加发现记录时上下文匹配文本与文件实际内容不一致，补丁未应用 | 1 | 读取文件尾部确认原文后重新追加 |
| `flutter analyze` 报 `PlayerSystemLevels.initialize` 未处理 `updateShowSystemUI` Future | 1 | 用 `unawaited` 明确声明系统提示切换为后台操作 |
| 新增手势测试用固定屏幕坐标启动拖动，未命中实际 widget 边界，垂直/水平回调为空 | 1 | 改用 `tester.getRect(find.byType(PlayerGestureLayer))` 计算测试起点并用 `dragFrom` |
| 改用 `dragFrom` 后测试高度受布局约束影响，且双击识别器的定时器尚未完成 | 2 | 用实际 widget 高度计算期望值，并在拖动后 pump 消化识别器定时器 |
| 垂直拖动的 gesture arena 会消耗触摸 slop，实际增量小于原始拖动距离，精确值断言不稳定 | 3 | 断言左右分区的方向和幅度对称性，不把 slop 当作业务逻辑 |
